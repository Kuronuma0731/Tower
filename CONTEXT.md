# Tower

一款商業品質的 Unity 手機遊戲：單人、爬塔策略 RPG / 地城探索，核心是**資源管理解謎**而非戰鬥爽度。Android + iOS，手機優先。

這份文件記錄兩件事：**共用語言**（讓人與 agent 用同一組詞討論）與**已鎖定的決策**（含理由）。改動任何一條「已鎖定」的決策前，先在這裡更新，再改程式。

---

## 共用語言

**塔 (Tower)**
整個遊戲空間。由連續編號的**樓層**組成，玩家從 1F 向上爬。一座塔即一個完整的通關體驗。
_避免_：關卡、地圖、dungeon

**樓層 (Floor)**
塔的一層，一張獨立的網格地圖。樓層是**存檔與回溯的基本單位**——進入樓層時自動快照。
_避免_：關卡、房間、stage

**格子 (Tile)**
樓層網格的最小單位。玩家、怪物、道具、門都佔據整數格。移動一律以格為單位，沒有半格。

**碰撞戰 (Collision Battle)**
玩家移動到怪物所在格時**立即結算**的戰鬥。結果完全確定，可在戰前精算。這是遊戲的預設戰鬥形式，也是「資源管理解謎」成立的前提。
_避免_：普通戰鬥、快速戰鬥

**指令戰 (Command Battle)**
進入獨立流程的回合制戰鬥，玩家逐回合選擇技能 / 道具 / 逃跑。僅 Boss 與守關精英使用。結果**不可預先精算**。
_避免_：正式戰鬥、Boss 戰（Boss 是怪物的分類，不是戰鬥形式）

**戰鬥結算器 (CombatResolver)**
兩種戰鬥共用的**唯一數值真相來源**。碰撞戰與指令戰只是它的兩種表現層。任何傷害公式只能存在於這裡。

**傷害預覽 (Damage Preview)**
在玩家撞上怪物**之前**顯示的預期 HP 損失。只對碰撞戰有效。這不是 QoL，是核心 UI——沒有它，玩家必須用紙筆算，等同勸退。

**怪物手冊 (Bestiary)**
已遭遇怪物的數值與能力列表，隨時可查。與**傷害預覽**共同構成玩家的計算依據。

**軟鎖 (Soft-lock)**
玩家進入了「無法通關但遊戲仍可繼續」的狀態——例如浪費鑰匙、血量耗損過度。本類型的頭號死因：**發生在 8F，玩家到 20F 才發現**。所有防護機制都圍繞這個詞。

**樓層快照 (Floor Snapshot)**
進入樓層瞬間的完整存檔。防**軟鎖**的**外層**保險：玩家永遠能退回任一已達樓層的入口。它處理的是「這層根本不該進」，處理不了「在這層裡犯的錯」——後者屬於**步數回溯**。

**步數回溯 (Undo)**
回退最近 N 步操作的能力，涵蓋移動、開門、戰鬥、購買。防**軟鎖**的**內層**手段，也是本類型玩家的既定預期。它才是真正處理「浪費一把鑰匙」這類最常見軟鎖的機制；只有**樓層快照**而沒有它，玩家在一層玩了 15 分鐘後失誤，代價是整整 15 分鐘——手機上這是棄坑級的挫折。

**可達性驗證器 (Reachability Solver)**
開發工具。自動驗證「以樓層入口的資源，是否存在一條路徑抵達出口 / Boss」。只覆蓋**碰撞戰**——**指令戰**的結果取決於玩家操作，算不出來。

因此驗證器的合約止於 Boss 門口：它驗證的是「抵達 Boss 時，資源 ≥ 該 Boss 標定的最低門檻」，而不是「打得贏」。Boss 本身的平衡由另一支**壓力測試**處理——固定幾組玩家操作品質（最佳解 / 中等 / 亂打），跑出勝率區間。兩者加起來才覆蓋完整路徑。

---

## 已鎖定的決策

### D1 — 混合戰鬥制

一般怪物走**碰撞戰**，Boss 與守關精英走**指令戰**。

**判定方式**：怪物 ScriptableObject 上的 `usesCommandBattle` flag。**不要在程式裡寫死「Boss 才進指令戰」**——資料驅動，之後才調得動。

**比例上限**：指令戰佔**遊玩時間** **15% 以下**（輔助指標：場次 5% 以下）。

單位是**時間**不是場次，因為一場指令戰的耗時可能是碰撞戰的 20 倍——5% 的場次可以輕易吃掉一半的遊玩時間，用場次當約束等於沒約束到想約束的東西。超過這個比例，「爬塔」會退化成「打怪」，兩種樂趣互相稀釋。

**理由**：碰撞戰保住魔塔類型的節奏與可計算性；指令戰讓 Boss 有戲劇張力與決策深度。

**代價（已接受）**：
1. 兩套戰鬥系統，MVP 時程 +50%
2. **傷害預覽**在 Boss 處失效，玩家失去可計算性
3. **可達性驗證器**只能覆蓋一半

**必要防護**：**指令戰前必須自動存檔，且必須可撤退。** 這條不能省——否則玩家可能耗光資源走到 Boss 前，打不過也回不去，這是把**軟鎖**放大到最痛的位置。

**每個 Boss 必須標定「入場資源門檻」**——這是**可達性驗證器**能驗證的邊界（見詞條），也是關卡設計時的明確合約。

### D2 — 買斷制

無廣告、無體力、無 live-ops、無每日登入、無 Battle Pass。

**形態**：前 5–10 層免費，一次性 IAP 解鎖全塔。體驗上仍是買斷，但避開手機買斷市場「沒人願意盲買」的問題。只需要**一個** IAP 商品，不是完整商業化系統。

**理由**：體力與獎勵式廣告會直接破壞**資源緊繃感**——那正是本作的核心樂趣。

**保留**：雲端存檔（換機必然發生）、輕量分析（僅「第幾層流失 / 通關率 / 平均時長」三項）。

### D3 — MVP 為 10–15 層

不是 50 層。目的是驗證核心樂趣與難度弧線，不是出貨。

**理由**：手工解謎樓層是本專案最大的隱形成本。在關卡編輯器成熟前量產樓層，等於把返工成本乘上樓層數。

### D4 — 不做多人

連預留架構都不做。單人爬塔的資源管理解謎與任何多人形式都沒有共通設計目標，預留只會產生純負值的抽象層。

### D5 — 不做程序生成樓層

與手工解謎的設計目標直接衝突。**樓層**的樂趣來自「設計者藏了一條路」，隨機生成無法提供。

### D6 — 出貨規模為 25–30 層

免費 **8 層**（付費牆前必須完整呈現核心循環：鑰匙抉擇、傷害預覽、至少一場指令戰），付費解鎖其餘 **20 層上下**。

**理由**：買斷制玩家對內容量有硬性期待，10–15 層撐不起一個付費商品；50 層則讓手工關卡量產成本失控。25–30 層是「內容量站得住 × 量產不失控」的交點。這條決策是所有時程估算的基準——沒有它，每個估算都是猜的。

（D3 的 MVP 10–15 層不變：MVP 是驗證用的，不是出貨版。D2 的「前 5–10 層免費」自此定為 8 層。）

### D7 — 防軟鎖採雙層機制

**步數回溯**（內層，回退最近 N 步）+ **樓層快照**（外層，退回任一已達樓層入口）。定義與分工見詞條。

**理由**：兩者防的是不同的軟鎖。只做快照，最常見的「同層內浪費鑰匙」防不到；只做 undo，跨層的深度誤入防不到。

**對架構的硬性要求**：undo 要成立，所有狀態變更必須走**指令模式**（每個操作是一個可逆的 command）。這必須從 `Core` 第一天就做——事後補裝等於重寫。

**邊界**：指令戰**內**的回合不可 undo（否則 Boss 戰變成試錯窮舉，戲劇張力歸零）；撤退回地圖後，「進入戰鬥」這一步整體可回溯。

---

## 待決事項

- 成長模型：等級制 vs 純道具成長（影響**戰鬥結算器**的公式形狀）
- 鑰匙分層：是否採用經典黃/藍/紅三層
- 輸入方式：點擊尋路 vs 虛擬方向鍵。關鍵差異：點擊尋路可能**自動走進怪物**，造成非預期的資源損失——要嘛尋路永遠繞開怪物（怪物必須點擊確認才打），要嘛選方向鍵。這是選擇的核心理由，不是操作手感問題
- 螢幕方向：直向 / 橫向 / 兩者
